import styled from "styled-components";

const Font = styled.a`
    font-family: 'Glory';
    font-weight: bold;
    font-size: 2em;
    text-decoration: none;
    color: #3A20E2;
    transition: background-color, 0.5s;

    &:hover{
        color: #ffffff;
    }
`

export { Font }